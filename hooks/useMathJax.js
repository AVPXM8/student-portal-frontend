"use client";

import { useEffect } from 'react';

const useMathJax = (dependencies, containerRef) => {
  useEffect(() => {
    // MathJax has been fully removed in favor of high-performance KaTeX SSR.
  }, []);
};

export default useMathJax;